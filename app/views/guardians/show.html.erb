<%= render @guardian %>

<% if @guardian.children.present? %>
  <table>
    <thead>
    <tr>
      <th>Child Name</th>
      <th>Check In</th>
      <th>Check Out</th>
    </tr>
    </thead>

    <tbody>
      <% @guardian.children.each do |child| %>
        <tr>
          <td><%= child.first_name + ' ' + child.last_name%></td>
          <td><%= button_to 'Check In', events_path(:event => { :child_id => child.id, :guardian_id => @guardian.id, :event_type => true }) %></td>
          <td><%= button_to 'Check Out', events_path(:event => { :child_id => child.id, :guardian_id => @guardian.id, :event_type => 0 }) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>